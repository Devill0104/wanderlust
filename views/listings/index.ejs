<% layout("/layouts/boilerplate.ejs") %>
<body>
    <h2>All Listings</h2>
    <% for(let listing of allListings){ %>
        <ul>
            <a href="/listings/<%= listing._id%>"> 
                <li> <%= listing.title %></li> 
            </a>
        </ul>
    <% } %>
</body>